networks:
  freqtrade:
    driver: bridge
services:
  freqtrade:
    image: freqtradeorg/freqtrade:stable
    # image: freqtradeorg/freqtrade:develop
    # Use plotting image
    # image: freqtradeorg/freqtrade:develop_plot
    # # Enable GPU Image and GPU Resources (only relevant for freqAI)
    # # Make sure to uncomment the whole deploy section
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           count: 1
    #           capabilities: [gpu]
    # Build step - only needed when additional dependencies are needed
    # build:
    #   context: .
    #   dockerfile: "./docker/Dockerfile.custom"
    restart: unless-stopped
    container_name: freqtrade
    volumes:
      - "/home/warlord/freqtrader/user_data:/freqtrade/user_data"
    # Expose api on port 8080 (localhost only)
    # Please read the https://www.freqtrade.io/en/stable/rest-api/ documentation
    # for more information.
    ports:
      - "58080:8080"
    # Default command used when running `docker compose up`
    command: >
      trade
      --logfile /freqtrade/user_data/logs/freqtrade.log
      --db-url sqlite:////freqtrade/user_data/tradesv3.sqlite
      --config /freqtrade/user_data/config.json
      --strategy TestStrategy -v
    networks:
      - freqtrade


  freqtrade_elliotwaves:
    image: freqtradeorg/freqtrade:stable
    # image: freqtradeorg/freqtrade:develop
    # Use plotting image
    # image: freqtradeorg/freqtrade:develop_plot
    # # Enable GPU Image and GPU Resources (only relevant for freqAI)
    # # Make sure to uncomment the whole deploy section
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           count: 1
    #           capabilities: [gpu]
    # Build step - only needed when additional dependencies are needed
    # build:
    #   context: .
    #   dockerfile: "./docker/Dockerfile.custom"
    restart: unless-stopped
    container_name: freqtrade_elliotwaves
    volumes:
      - "/home/warlord/freqtrader/user_data:/freqtrade/user_data"
    # Expose api on port 8080 (localhost only)
    # Please read the https://www.freqtrade.io/en/stable/rest-api/ documentation
    # for more information.
    ports:
      - "58081:8080"
    # Default command used when running `docker compose up`
    command: >
      trade
      --logfile /freqtrade/user_data/logs/freqtrade_elliott.log
      --db-url sqlite:////freqtrade/user_data/tradesv3_elliott.sqlite
      --config /freqtrade/user_data/config_elliott.json
      --strategy ElliottWaveStrategy -v
    networks:
      - freqtrade

  freqtrade_cuttingedge:
    image: freqtradeorg/freqtrade:stable
    # image: freqtradeorg/freqtrade:develop
    # Use plotting image
    # image: freqtradeorg/freqtrade:develop_plot
    # # Enable GPU Image and GPU Resources (only relevant for freqAI)
    # # Make sure to uncomment the whole deploy section
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           count: 1
    #           capabilities: [gpu]
    # Build step - only needed when additional dependencies are needed
    # build:
    #   context: .
    #   dockerfile: "./docker/Dockerfile.custom"
    restart: unless-stopped
    container_name: freqtrade_cuttingedge
    volumes:
      - "/home/warlord/freqtrader/user_data:/freqtrade/user_data"
    # Expose api on port 8080 (localhost only)
    # Please read the https://www.freqtrade.io/en/stable/rest-api/ documentation
    # for more information.
    ports:
      - "58082:8080"
    # Default command used when running `docker compose up`
    command: >
      trade
      --logfile /freqtrade/user_data/logs/freqtrade_cuttingedge.log
      --db-url sqlite:////freqtrade/user_data/tradesv3_cuttingedge.sqlite
      --config /freqtrade/user_data/config_cuttingedge.json
      --strategy CuttingEdgeRegimeBot -v
    networks:
      - freqtrade

  freqtrade_shorttermscalper:
    image: freqtradeorg/freqtrade:stable
    # image: freqtradeorg/freqtrade:develop
    # Use plotting image
    # image: freqtradeorg/freqtrade:develop_plot
    # # Enable GPU Image and GPU Resources (only relevant for freqAI)
    # # Make sure to uncomment the whole deploy section
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           count: 1
    #           capabilities: [gpu]
    # Build step - only needed when additional dependencies are needed
    # build:
    #   context: .
    #   dockerfile: "./docker/Dockerfile.custom"
    restart: unless-stopped
    container_name: freqtrade_shorttermscalper
    volumes:
      - "/home/warlord/freqtrader/user_data:/freqtrade/user_data"
    # Expose api on port 8080 (localhost only)
    # Please read the https://www.freqtrade.io/en/stable/rest-api/ documentation
    # for more information.
    ports:
      - "58083:8080"
    # Default command used when running `docker compose up`
    command: >
      trade
      --logfile /freqtrade/user_data/logs/freqtrade_shorttermscalper.log
      --db-url sqlite:////freqtrade/user_data/tradesv3_shorttermscalper.sqlite
      --config /freqtrade/user_data/config_shorttermscalper.json
      --strategy ShortTermScalper -v
    networks:
      - freqtrade

  freqtrade_contrarian:
    image: registry.martin.whtn.adminthis.be/freqtrader-bot:latest
    #image: freqtradeorg/freqtrade:develop
    # Use plotting image
    # image: freqtradeorg/freqtrade:develop_plot
    # # Enable GPU Image and GPU Resources (only relevant for freqAI)
    # # Make sure to uncomment the whole deploy section
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           count: 1
    #           capabilities: [gpu]
    # Build step - only needed when additional dependencies are needed
    # build:
    #   context: .
    #   dockerfile: "./docker/Dockerfile.custom"
    volumes:
      - "/home/warlord/freqtrader/user_data:/freqtrade/user_data"
    restart: unless-stopped
    container_name: freqtrade_contrarian
    # Expose api on port 8080 (localhost only)
    # Please read the https://www.freqtrade.io/en/stable/rest-api/ documentation
    # for more information.
    ports:
      - "58084:8080"
    # Default command used when running `docker compose up`
    command: >
      trade
      --logfile /freqtrade/user_data/logs/freqtrade_contrarian.log
      --db-url sqlite:////freqtrade/user_data/freqtrade_contrarian.sqlite
      --config /freqtrade/user_data/config_contrarian.json
      --strategy ContrarianMeanReversion -v
    networks:
      - freqtrade

  freqtrade_5minscalper:
    image: registry.martin.whtn.adminthis.be/freqtrader-bot:latest
    # image: freqtradeorg/freqtrade:develop
    # Use plotting image
    # image: freqtradeorg/freqtrade:develop_plot
    # # Enable GPU Image and GPU Resources (only relevant for freqAI)
    # # Make sure to uncomment the whole deploy section
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           count: 1
    #           capabilities: [gpu]
    # Build step - only needed when additional dependencies are needed
    # build:
    #   context: .
    #   dockerfile: "./docker/Dockerfile.custom"
    restart: unless-stopped
    container_name: freqtrade_5minscalper
    volumes:
      - "/home/warlord/freqtrader/user_data:/freqtrade/user_data"
    # Expose api on port 8080 (localhost only)
    # Please read the https://www.freqtrade.io/en/stable/rest-api/ documentation
    # for more information.
    ports:
      - "58085:8080"
    # Default command used when running `docker compose up`
    command: >
      trade
      --logfile /freqtrade/user_data/logs/freqtrade_5minscalper.log
      --db-url postgresql://warlord:warlord@192.168.64.250:5432/freqtrade_2
      --config /freqtrade/user_data/config_5minscalper.json
      --strategy FiveMinScalper
    networks:
      - freqtrade



  freqtrade_XGBooster:
    image: registry.martin.whtn.adminthis.be/freqtrader-bot:latest
    # image: freqtradeorg/freqtrade:develop
    # Use plotting image
    # image: freqtradeorg/freqtrade:develop_plot
    # # Enable GPU Image and GPU Resources (only relevant for freqAI)
    # # Make sure to uncomment the whole deploy section
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           count: 1
    #           capabilities: [gpu]
    # Build step - only needed when additional dependencies are needed
    # build:
    #   context: .
    #   dockerfile: "./docker/Dockerfile.custom"
    restart: unless-stopped
    container_name: freqtrade_XGBooster
    volumes:
      - "/home/warlord/freqtrader/user_data:/freqtrade/user_data"
    # Expose api on port 8080 (localhost only)
    # Please read the https://www.freqtrade.io/en/stable/rest-api/ documentation
    # for more information.
    ports:
      - "58086:8080"
    # Default command used when running `docker compose up`
    command: >
      trade
      --logfile /freqtrade/user_data/logs/freqtrade_XGBooster.log
      --db-url sqlite:////freqtrade/user_data/freqtrade_XGBooster.sqlite
      --config /freqtrade/user_data/config_XGBooster.json
      --freqaimodel XGBoostClassifier
      --strategy XGBoosterStrategy -v
    networks:
      - freqtrade



  freqtrade_XGboostClassifier:
    image: registry.martin.whtn.adminthis.be/freqtrader-bot:latest
    # image: freqtradeorg/freqtrade:develop
    # Use plotting image
    # image: freqtradeorg/freqtrade:develop_plot
    # # Enable GPU Image and GPU Resources (only relevant for freqAI)
    # # Make sure to uncomment the whole deploy section
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           count: 1
    #           capabilities: [gpu]
    # Build step - only needed when additional dependencies are needed
    # build:
    #   context: .
    #   dockerfile: "./docker/Dockerfile.custom"
    restart: unless-stopped
    container_name: freqtrade_XGboostClassifier
    volumes:
      - "/home/warlord/freqtrader/user_data:/freqtrade/user_data"
    # Expose api on port 8080 (localhost only)
    # Please read the https://www.freqtrade.io/en/stable/rest-api/ documentation
    # for more information.
    ports:
      - "58087:8080"
    # Default command used when running `docker compose up`
    command: >
      trade
      --logfile /freqtrade/user_data/logs/XGboostClassifier.log
      --db-url postgresql://warlord:warlord@192.168.64.250:5432/freqtrader_XGBoostClassifier
      --config /freqtrade/user_data/config_XGBoostClassifier.json
      --freqaimodel XGBoostClassifier
      --strategy XGBoostClassifierStrategy
    networks:
      - freqtrade


  freqtrade_freqai:
    image: registry.martin.whtn.adminthis.be/freqtrader-bot:latest
    # image: freqtradeorg/freqtrade:develop
    # Use plotting image
    # image: freqtradeorg/freqtrade:develop_plot
    # # Enable GPU Image and GPU Resources (only relevant for freqAI)
    # # Make sure to uncomment the whole deploy section
    # deploy:
    #   resources:
    #     reservations:
    #       devices:
    #         - driver: nvidia
    #           count: 1
    #           capabilities: [gpu]
    # Build step - only needed when additional dependencies are needed
    # build:
    #   context: .
    #   dockerfile: "./docker/Dockerfile.custom"
    restart: unless-stopped
    container_name: freqtrade_freqai
    volumes:
      - "/home/warlord/freqtrader/user_data:/freqtrade/user_data"
    # Expose api on port 8080 (localhost only)
    # Please read the https://www.freqtrade.io/en/stable/rest-api/ documentation
    # for more information.
    ports:
      - "58088:8080"
    # Default command used when running `docker compose up`
    command: >
      trade
      --logfile /freqtrade/user_data/logs/freqtrade_freqai.log
      --db-url postgresql://warlord:warlord@192.168.64.250:5432/freqtrade
      --config /freqtrade/user_data/config_freqai.json
      --freqaimodel XGBoostRegressor
      --strategy FreqaiExampleStrategy
    networks:
      - freqtrade


  freqtrade_FourierWavelet:
    build:
      context: .
      dockerfile: "./docker/Dockerfile.custom"
    image: freqtrader-fourierwavelet:latest
    restart: unless-stopped
    container_name: freqtrade_FourierWavelet
    volumes:
      - "/home/warlord/freqtrader/user_data:/freqtrade/user_data"
    ports:
      - "58089:8080"
    command: >
      trade
      --logfile /freqtrade/user_data/logs/freqtrade_FourierWavelet.log
      --db-url sqlite:////freqtrade/user_data/freqtrade_FourierWavelet.sqlite
      --config /freqtrade/user_data/config_FourierWavelet.json
      --freqaimodel XGBoostRegressor
      --strategy FourierWaveletStrategy -v
    networks:
      - freqtrade











